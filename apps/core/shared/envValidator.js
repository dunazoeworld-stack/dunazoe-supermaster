const ALWAYS_REQUIRED=["DATABASE_URL","JWT_SECRET","INTERNAL_SECRET"];const INSECURE=["change_this","secret","password","changeme","dunazoe_secret","dunazoe_internal"];
function validate(required=[],opts={}){const all=[...new Set([...ALWAYS_REQUIRED,...required])];const errors=[];for(const k of all){if(!process.env[k]||!process.env[k].trim())errors.push(`Missing: ${k}`);}if(process.env.NODE_ENV==="production"||opts.strict){for(const k of["JWT_SECRET","INTERNAL_SECRET"]){const v=(process.env[k]||"").toLowerCase();if(INSECURE.some(bad=>v.includes(bad)))errors.push(`SECURITY: ${k} contains placeholder`);if(v.length<32)errors.push(`SECURITY: ${k} must be >= 32 chars`);}}const svc=process.env.SERVICE_NAME||"service";if(errors.length){errors.forEach(e=>console.error(`[EnvValidator] ❌ ${e}`));process.exit(1);}console.log(`[EnvValidator] ✅ ${svc} env OK`);}
function get(key,fallback){const v=process.env[key];if(v===undefined&&fallback===undefined)throw new Error(`Required env var ${key} not set`);return v!==undefined?v:fallback;}
module.exports={validate,get};
